name: probe
on:
  workflow_dispatch:
  push:
    branches: [ "arena/**" ]
jobs:
  win:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: dotnet info
        run: dotnet --info
